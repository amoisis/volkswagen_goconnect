mode: ContinuousDelivery
tag-prefix: '[vV]'
branches:
  main:
    regex: ^main$
    increment: Patch
    is-main-branch: true
  feature:
    regex: ^features?[/-]
    increment: Minor
  hotfix:
    regex: ^hotfix(es)?[/-]
    increment: Patch